import React, { StrictMode } from 'react';

// ====

const LoginContainer = () => {
    return (
        <StrictMode>
            <h1>It works!</h1>
        </StrictMode>
    );
};

// ====

export default LoginContainer;
