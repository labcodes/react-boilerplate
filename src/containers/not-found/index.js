import React, { StrictMode } from 'react';

// ====

const NotFound = () => {
    return(
        <StrictMode>
            <h1>NotFound</h1>
        </StrictMode>
    );
};

// ====

export default NotFound;
